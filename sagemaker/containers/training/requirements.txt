torch>=2.1.0
transformers>=4.35.0
accelerate>=0.26.0
datasets>=2.14.0
wandb>=0.15.0
boto3>=1.28.0
numpy>=1.24.0

# Remove DeepSpeed - we're not using it anymore
# deepspeed>=0.12.0  # REMOVED
# mpi4py>=3.1.0      # REMOVED  
# ninja>=1.10.0      # REMOVED

# Keep essential dependencies
packaging>=21.0
psutil>=5.9.0
tokenizers>=0.15.0

# Add safetensors for better model loading
safetensors>=0.3.0

# Add flash-attention if available (optional)
# flash-attn>=2.0.0  # Uncomment if you want flash attention