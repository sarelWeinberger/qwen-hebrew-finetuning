# Core dependencies
torch>=2.0.0
transformers>=4.30.0
datasets>=2.10.0
accelerate>=0.20.0
deepspeed>=0.9.3
tensorboard>=2.10.0
wandb>=0.15.0

# Hyperparameter optimization
optuna>=3.0.0
colorlog>=6.7.0

# Data processing
numpy>=1.22.0
pandas>=1.5.0
scikit-learn>=1.0.0
tqdm>=4.65.0
python-Levenshtein>=0.21.0

# Utilities
requests>=2.28.0
packaging>=20.0
pyyaml>=6.0
boto3>=1.38.28

#for aws: 
# First, install the specific version of botocore required by aiobotocore
#pip install "botocore>=1.37.0,<1.37.2"

# Install nvidia-ml-py3 as required by autogluon-multimodal
#pip install "nvidia-ml-py3>=7.352.0,<8.0"

# Install the correct version of transformers
#pip install "transformers[sentencepiece]>=4.38.0,<4.50"

# Install the correct version of coreforecast
#pip install "coreforecast>=0.0.12,<0.0.16"

# Update dill and multiprocess
#pip install "dill>=0.4.0"
#pip install "multiprocess>=0.70.18"

# Install the specific version of sparkmagic
#pip install "sparkmagic==0.22.0"
